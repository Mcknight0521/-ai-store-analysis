(()=>{
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=s=>(s??'').toString().replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const text=(s,r=document)=>$(s,r)?.textContent?.replace(/\s+/g,' ').trim()||'';
const icon={grid:'<svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="7" rx="2"/><rect x="14" y="3" width="7" height="7" rx="2"/><rect x="3" y="14" width="7" height="7" rx="2"/><rect x="14" y="14" width="7" height="7" rx="2"/></svg>',spark:'<svg viewBox="0 0 24 24"><path d="m12 3 1.6 4.4L18 9l-4.4 1.6L12 15l-1.6-4.4L6 9l4.4-1.6L12 3Z"/><path d="m18 15 .8 2.2L21 18l-2.2.8L18 21l-.8-2.2L15 18l2.2-.8L18 15Z"/></svg>',money:'<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M15 8.5c-.7-.7-1.8-1.1-3-1.1-1.8 0-3 1-3 2.4 0 3.5 6 1.5 6 5 0 1.5-1.3 2.7-3.2 2.7-1.4 0-2.6-.5-3.4-1.3M12 5v14"/></svg>',alert:'<svg viewBox="0 0 24 24"><path d="M12 3 2.8 20h18.4L12 3Z"/><path d="M12 9v5m0 3h.01"/></svg>',arrow:'<svg viewBox="0 0 24 24"><path d="M5 12h14m-5-5 5 5-5 5"/></svg>'};
function source(id){return document.getElementById(id)}
function metricData(){const p=source('executive');return p?$$('.ex513-kpi',p).map((n,i)=>({label:text('span',n)||`指標 ${i+1}`,value:text('b,strong',n)||'—',note:text('small',n)})):[];}
function rows(root,selector,max=6){return root?$$(selector,root).slice(0,max).map((r,i)=>({rank:i+1,name:text('.ex513-name b,.v57-main b,.ar519-name b,.focus b',r)||text('.ex513-name,.v57-main,.ar519-name,.focus',r)||'—',meta:text('.ex513-name small,.v57-main small,.ar519-meta,.focus small',r),value:text('.ex513-val,.v57-value,.ar519-value b,.focus-tag',r)||'—'})):[];}
function barList(items,tone='blue'){return `<div class="v17-ranklist ${tone}">${items.map((x,i)=>`<div class="v17-rankrow"><span class="no">${x.rank}</span><div class="copy"><b>${esc(x.name)}</b><small>${esc(x.meta)}</small><div class="track"><i style="width:${Math.max(20,100-i*11)}%"></i></div></div><strong>${esc(x.value)}</strong></div>`).join('')}</div>`}
function parseEvents(){const panel=$('#executive .event-panel');if(!panel)return {events:[],summary:''};const raw=panel.textContent?.replace(/\s+/g,' ').trim()||'';const events=[];const re=/(\d{2}\/\d{2})([^—]*?)當日事件/g;let m;while((m=re.exec(raw))){let label=m[2].trim().replace(/^[：:、\-\s]+/,'').replace(/[。；;]+$/,'');if(label&&!events.some(e=>e.date===m[1]&&e.label===label))events.push({date:m[1],label});}const c=raw.match(/停班停課\s*(\d+)\s*筆/),t=raw.match(/颱風影響\s*(\d+)\s*筆/);const parts=[];if(c)parts.push(`停班停課 ${c[1]} 筆`);if(t)parts.push(`颱風影響 ${t[1]} 筆`);return {events,summary:parts.join(' · ')};}
function executiveHTML(){const base=$('#opxView');if(!base)return '';const wrap=document.createElement('div');wrap.innerHTML=base.innerHTML;const ev=parseEvents(),old=$('.opx-context-item',wrap);if(old){const section=old.closest('.opx-card');if(section){section.classList.add('v17-event-card');section.innerHTML=`<div class="v17-cardhead"><span class="v17-icon amber">${icon.alert}</span><div><small>PERIOD CONTEXT</small><h3>期間事件</h3><p>只顯示會影響本期營運判讀的日期事件。</p></div>${ev.summary?`<em>${esc(ev.summary)}</em>`:''}</div>${ev.events.length?`<div class="v17-eventline">${ev.events.map(e=>`<div><time>${esc(e.date)}</time><b>${esc(e.label)}</b></div>`).join('')}</div>`:'<div class="v17-empty">本期沒有需要特別標示的事件。</div>'}`;}}
$$('.opx-card',wrap).forEach((c,i)=>c.classList.add('v17-lift',`v17-card-${i%4}`));return wrap.innerHTML;}
function overviewHTML(){const p=source('overview');if(!p)return '';const metrics=$$('.metric',p).slice(0,6).map((m,i)=>({label:text('span',m)||`結構 ${i+1}`,value:text('strong,b',m)||'—'}));const cards=$$('.v522-card',p),focusRows=rows(p,'.focus',6);return `<section class="v17-structure"><div class="v17-structure-copy"><span class="v17-icon">${icon.grid}</span><div><small>OPERATING STRUCTURE</small><h2>營運結構</h2><p>先看本期營運組成，再往下判讀變化與值得追查的訊號。</p></div></div><div class="v17-structure-grid">${metrics.length?metrics.map((m,i)=>`<article class="tone-${i%5}"><i></i><small>${esc(m.label)}</small><b>${esc(m.value)}</b><span>${i%2?'結構占比 / 變化':'本期核心指標'}</span></article>`).join(''):'<div class="v17-empty">匯入資料後顯示營運結構。</div>'}</div></section><section class="v17-insight-layout"><article class="v17-feature"><div class="v17-feature-top"><span class="v17-icon violet">${icon.spark}</span><div><small>SMART SIGNAL</small><h3>本期重點洞察</h3></div></div><p>${esc(text('.v522-structure-head p,.panel.dark strong,.panel.dark p',p)||'依本期資料結構整理最值得注意的變化。')}</p>${focusRows.length?barList(focusRows,'violet'):'<div class="v17-empty">目前沒有可呈現的洞察項目。</div>'}</article><div class="v17-insight-cards">${cards.slice(0,4).map((c,i)=>`<article><span>${String(i+1).padStart(2,'0')}</span><div><small>INSIGHT</small><h4>${esc(text('b,h3,h4',c)||`營運觀察 ${i+1}`)}</h4><p>${esc(text('p',c)||text('small',c)||'依目前資料持續觀察。')}</p></div>${icon.arrow}</article>`).join('')||'<div class="v17-empty">等待更多結構資料。</div>'}</div></section>`;}
function analysisMoney(){const data=metricData(),want=['營業額','出清','報廢','損耗'],out=[];for(const k of want){const x=data.find(v=>v.label.includes(k)&&!v.value.includes('%'));if(x&&!out.some(o=>o.label===x.label))out.push(x)}return out.slice(0,4);}
function analysisHTML(){const p=source('analysis');if(!p)return '';const amounts=analysisMoney(),cards=$$('.analysis-v57-card',p).slice(0,3);return `<section class="v17-money"><div class="v17-money-head"><div><small>MONEY OVERVIEW</small><h2>金額總覽</h2><p>先掌握本期金額規模，再往下拆解營業額、出清與報廢。</p></div><span class="v17-icon green">${icon.money}</span></div><div class="v17-money-grid">${amounts.length?amounts.map((x,i)=>`<article class="tone-${i}"><small>${esc(x.label)}</small><b>${esc(x.value)}</b><span>${esc(x.note||'本期實際金額')}</span><i></i></article>`).join(''):'<div class="v17-empty">匯入資料後顯示本期金額。</div>'}</div></section><div class="v17-analysis-flow">${cards.map((c,i)=>{const title=text('.analysis-v57-head b',c)||['營業額分析','出清分析','報廢分析'][i],sub=text('.analysis-v57-head small',c),k=text('.v57-kpi strong',c),kl=text('.v57-kpi span',c),rr=rows(c,'.v57-row',10);return `<section class="v17-analysis-card tone-${i}"><div class="v17-analysis-head"><span class="step">0${i+1}</span><div><small>${i===0?'SALES':i===1?'CLEARANCE':'WASTE'}</small><h3>${esc(title)}</h3><p>${esc(sub)}</p></div><div class="big"><small>${esc(kl)}</small><b>${esc(k||'—')}</b></div></div>${barList(rr,i===0?'blue':i===1?'amber':'red')}</section>`}).join('')}</div>`;}
function current(){return $('#opxApp .opx-nav button.active')?.dataset.page||'executive'}
function ensureStable(){let v=$('#v17StableView');if(v)return v;const base=$('#opxView');if(!base)return null;v=document.createElement('div');v.id='v17StableView';base.after(v);return v;}
let lastHTML='',lastPage='',timer=0;
function build(page){if(page==='executive')return executiveHTML();if(page==='overview')return overviewHTML();if(page==='analysis')return analysisHTML();return $('#opxView')?.innerHTML||'';}
function render(force=false){const stable=ensureStable();if(!stable)return;const page=current(),html=build(page);if(!force&&page===lastPage&&html===lastHTML)return;lastPage=page;lastHTML=html;stable.innerHTML=html;stable.dataset.page=page;}
function schedule(force=false,delay=260){clearTimeout(timer);timer=setTimeout(()=>render(force),delay)}
function init(){const app=$('#opxApp');if(!app||!$('#opxView')){setTimeout(init,80);return}document.body.classList.add('v17-ready','v17-stable');ensureStable();render(true);app.addEventListener('click',e=>{if(e.target.closest('.opx-nav button')){lastHTML='';lastPage='';schedule(true,80)}});['executive','overview','analysis','anomaly','improve','report'].forEach(id=>{const el=source(id);if(!el)return;new MutationObserver(()=>schedule(false,320)).observe(el,{subtree:true,childList:true,characterData:true})});}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();